name = "doublej-nav"
main = "src/index.ts"
compatibility_date = "2026-07-01"
workers_dev = true

# GH_TOKEN is a secret, never a var:
#   wrangler secret put GH_TOKEN
# Fine-grained PAT on doublej/doublej only, Actions: read and write. Nothing else.
